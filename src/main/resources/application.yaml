server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/freshguard
    username: postgres
    password: 123
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

jwt:
  secret: freshguard-secret-key-2026-bravo-market-azerbaijan-secure
  expiration: 86400000